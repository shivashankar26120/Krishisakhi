# This MUST match the variable name read in src/services/api.js
# The Vercel dashboard environment variable MUST be named VITE_API_BASE_URL
# (not VITE_API_URL — that name is wrong and causes localhost fallback in production)
VITE_API_BASE_URL=https://resourceful-stillness-production-1007.up.railway.app
